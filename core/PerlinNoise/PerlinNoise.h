#pragma once
#include <vector>
#include "Vec2.h"
#include "Vec4.h"
#include "PRNG.h"

namespace WorldMaker {
    //
    // class PerlinNoise {
    //     int m_height;
    //     int m_width;
    //
    //
    //     const Vec2 TopLeft = Vec2(0.5, -0.5);
    //     const Vec2 TopRight = Vec2(-0.5, -0.5);
    //     const Vec2 BottomLeft = Vec2(0.5, 0.5);
    //     const Vec2 BottomRight = Vec2(-0.5, 0.5);
    //     public:
    //     std::vector<std::vector<Vec2>> getVectors(PRNG &prng);
    //     std::vector<std::vector<Vec4>> getGradients(std::vector<std::vector<Vec2>> &vectors);
    //     PerlinNoise(int width, int height, PRNG &prng);
    //     void getPerlinNoise(PRNG &prng);
    //
    // public:
    //     std::vector<std::vector<double>> m_perlinNoise;
    //
    // };
}
